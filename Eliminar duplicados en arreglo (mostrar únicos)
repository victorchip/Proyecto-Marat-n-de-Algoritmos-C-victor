#include <iostream>
using namespace std;

int main() {
    int N;
    cout << "N: ";
    cin >> N;
    int a[1000];
    for (int i=0;i<N;++i) cin >> a[i];
    for (int i=0;i<N;++i) {
        bool seen = false;
        for (int j=0;j<i;++j) if (a[j]==a[i]) { seen = true; break; }
        if (!seen) cout << a[i] << " ";
    }
    cout << endl;
    return 0;
}

