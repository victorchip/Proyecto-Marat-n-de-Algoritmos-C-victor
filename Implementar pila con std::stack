#include <iostream>
#include <stack>
using namespace std;

int main(){
    stack<int> st;
    int n; cin>>n;
    for(int i=0;i<n;i++){ int x; cin>>x; st.push(x);}

    cout<<"Desapilando: ";
    while(!st.empty()){ cout<<st.top()<<" "; st.pop();}
    cout<<endl;
    return 0;
}
