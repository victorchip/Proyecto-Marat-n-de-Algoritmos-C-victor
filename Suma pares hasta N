#include <iostream>
using namespace std;

int main() {
    int N;
    cout << "Ingresa N: ";
    cin >> N;
    int suma = 0;
    for (int i = 2; i <= N; i += 2) suma += i;
    cout << "Suma pares: " << suma << endl;
    return 0;
}
