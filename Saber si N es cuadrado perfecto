#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int n; cin >> n;
    int r = (int) sqrt(n);
    cout << (r*r == n ? "Cuadrado perfecto\n" : "No es cuadrado perfecto\n");
    return 0;
}


