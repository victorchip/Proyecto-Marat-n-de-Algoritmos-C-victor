#include <iostream>
using namespace std;

int main() {
    int a, b;
    cout << "Ingresa a y b: ";
    cin >> a >> b;
    while (b != 0) {
        int r = a % b;
        a = b;
        b = r;
    }
    cout << "MCD: " << (a < 0 ? -a : a) << endl;
    return 0;
}

