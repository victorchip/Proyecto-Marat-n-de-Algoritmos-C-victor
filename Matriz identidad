#include <iostream>
using namespace std;

int main() {
    int N; cout << "N: "; cin >> N;
    for (int i=0;i<N;++i) {
        for (int j=0;j<N;++j) cout << (i==j ? 1 : 0) << " ";
        cout << endl;
    }
    return 0;
}

