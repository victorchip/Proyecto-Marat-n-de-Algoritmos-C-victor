#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; ++i) cin >> a[i];
    if (next_permutation(a.begin(), a.end())) {
        for (int x : a) cout << x << ' ';
    } else {
        cout << "No existe siguiente permutación\n";
    }
}
