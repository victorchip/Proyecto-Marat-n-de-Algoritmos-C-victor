#include <bits/stdc++.h>
using namespace std;

int main(){
    int n; cin >> n;
    vector<double> v(n);
    for(double &x:v) cin >> x;
    sort(v.begin(), v.end());
    double med = (n%2 ? v[n/2] : (v[n/2-1]+v[n/2])/2.0);
    cout << "Mediana: " << fixed << setprecision(2) << med << "\n";
}

