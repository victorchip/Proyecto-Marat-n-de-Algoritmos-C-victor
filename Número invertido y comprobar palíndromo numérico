#include <iostream>
using namespace std;

int main() {
    int n; cin >> n;
    int orig = n, rev = 0;
    while (n) { rev = rev*10 + n%10; n /= 10; }
    cout << (rev == orig ? "Palíndromo\n" : "No palíndromo\n");
    return 0;
}

