#include <iostream>
using namespace std;

int main() {
    int N; cin >> N;
    int a[1000], v, cnt=0;
    for (int i=0;i<N;++i) cin >> a[i];
    cin >> v;
    for (int i=0;i<N;++i) if (a[i]==v) ++cnt;
    cout << "Ocurrencias: " << cnt << endl;
    return 0;
}
