#include <iostream>
using namespace std;

int sumaRec(int arr[], int n){
    if(n==0) return 0;
    return arr[n-1]+sumaRec(arr,n-1);
}

int main() {
    int arr[]={1,2,3,4}, n=4;
    cout << "Suma total: " << sumaRec(arr,n) << endl; // 10
    return 0;
}

