#include <iostream>
#include <string>
using namespace std;

int main() {
    string a, b; cin >> a >> b;
    if (a == b) cout << "Iguales\n";
    else if (a < b) cout << a << " < " << b << endl;
    else cout << a << " > " << b << endl;
    return 0;
}

