#include <iostream>
using namespace std;

int potencia(int base, int exp){
    if(exp==0) return 1;
    return base*potencia(base, exp-1);
}

int main() {
    int base=2, exp=5;
    cout << base << "^" << exp << " = " << potencia(base, exp) << endl; // 32
    return 0;
}

