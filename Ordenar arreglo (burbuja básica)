#include <iostream>
using namespace std;

int main() {
    int N; cin >> N;
    int a[1000];
    for (int i=0;i<N;++i) cin >> a[i];
    for (int i=0;i<N-1;++i)
        for (int j=0;j<N-1-i;++j)
            if (a[j] > a[j+1]) swap(a[j], a[j+1]);
    for (int i=0;i<N;++i) cout << a[i] << " ";
    cout << endl;
    return 0;
}


