#include <iostream>
#include <string>
using namespace std;

int main() {
    int n; cin >> n;
    if (n==0) { cout << "0\n"; return 0; }
    string b="";
    while (n>0) { b.push_back('0' + (n%2)); n/=2; }
    reverse(b.begin(), b.end());
    cout << b << endl;
    return 0;
}

