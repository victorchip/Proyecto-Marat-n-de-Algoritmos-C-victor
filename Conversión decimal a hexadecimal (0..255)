#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    int n; cin >> n;
    if (n<0 || n>255) { cout << "Fuera de rango\n"; return 0; }
    cout << hex << uppercase << n << endl;
    return 0;
}
