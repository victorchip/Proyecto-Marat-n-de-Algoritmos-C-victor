#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, k;
    cin >> n >> k;
    vector<int> mask(n, 0);
    fill(mask.begin(), mask.begin() + k, 1);
    do {
        for (int i = 0; i < n; ++i)
            if (mask[i]) cout << i + 1 << ' ';
        cout << '\n';
    } while (prev_permutation(mask.begin(), mask.end()));
}
