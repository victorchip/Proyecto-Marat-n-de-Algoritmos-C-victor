#include <iostream>
using namespace std;

int main() {
    double a, b, c;
    cout << "Ingresa tres números: ";
    cin >> a >> b >> c;
    double prom = (a + b + c) / 3.0;
    cout << "Promedio: " << prom << endl;
    return 0;
}

