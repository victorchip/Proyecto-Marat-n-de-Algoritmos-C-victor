#include <iostream>
using namespace std;

int main() {
    int N; cout << "N: "; cin >> N;
    int suma = 0;
    for (int i=1;i<=N;++i) suma += (i % 2 == 1 ? i : -i);
    cout << "Resultado: " << suma << endl;
    return 0;
}

